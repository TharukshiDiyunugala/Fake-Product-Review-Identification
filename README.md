# Fake Review Detection (Deceptive Opinion Spam Corpus)

## Overview
Detect fake product/hotel reviews using pre-trained transformer models (BERT, RoBERTa, XLNet).

## Repo structure
(brief structure here)

## Setup
```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
